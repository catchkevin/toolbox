<#
You should run this constantly. It tells you exactly what is happening in your workspace right now before you make a save point.
#>

git status

<#Use git status -s for a "Short" version. It uses a code like M (Modified) or ?? (Untracked) to give you a clean, one-line-per-file view.#>
